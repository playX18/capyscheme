# Unit tests for Capy library

This directory 1 to 1 replicates `lib/` directory and each file tests corresponding file in `lib/` directory.

To run tests, run `make test` in the root directory. To run with custom Capy binary, set `CAPY` environment variable.